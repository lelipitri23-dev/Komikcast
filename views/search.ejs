<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cari: <%= searchQuery %> - KomikCast Clone</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>body { font-family: 'Poppins', sans-serif; background-color: #141414; color: #cbd5e1; }</style>
</head>
<body>

  <%- include('partials/navbar') %>

  <div class="container mx-auto px-4 py-8 min-h-screen">
    
    <div class="border-b border-[#333] pb-4 mb-6">
      <h2 class="text-xl font-bold text-white">
        Hasil Pencarian: "<span class="text-blue-500 italic"><%= searchQuery %></span>"
      </h2>
      <p class="text-xs text-gray-500 mt-1">Ditemukan <%= mangas.length %> komik.</p>
    </div>

    <% if (mangas.length > 0) { %>
      <div class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">
        <% mangas.forEach(manga => { %>
          <div class="bg-[#1e1e1e] rounded-lg overflow-hidden shadow-lg border border-[#2a2a2a] group hover:border-blue-500/50 transition duration-300">
            
            <div class="relative aspect-[3/4] overflow-hidden">
              <a href="/komik/<%= manga.slug %>">
                <img src="<%= manga.coverImage %>" alt="<%= manga.title %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-500">
              </a>
              <div class="absolute top-2 left-2 bg-yellow-500 text-black text-[10px] font-bold px-1.5 py-0.5 rounded shadow">
                <i class="fas fa-star text-[9px]"></i> <%= manga.rating %>
              </div>
              <div class="absolute top-2 right-2 bg-black/70 text-white text-[9px] font-bold px-1.5 py-0.5 rounded uppercase">
                <%= manga.type %>
              </div>
            </div>

            <div class="p-3">
              <a href="/komik/<%= manga.slug %>">
                <h3 class="font-bold text-xs text-white leading-snug line-clamp-2 hover:text-blue-500 transition mb-2">
                  <%= manga.title %>
                </h3>
              </a>
              <div class="text-[10px] text-gray-500 flex justify-between items-center border-t border-[#333] pt-2">
                <span><%= manga.status %></span>
                <span>Update: <%= new Date(manga.lastUpdated).toLocaleDateString('id-ID', {day: 'numeric', month: 'short'}) %></span>
              </div>
            </div>

          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="text-center py-20 bg-[#1e1e1e] rounded border border-[#333]">
        <i class="fas fa-search text-4xl text-gray-600 mb-4"></i>
        <h3 class="text-lg font-bold text-white">Komik tidak ditemukan</h3>
        <p class="text-gray-500 text-sm">Coba cari dengan kata kunci lain.</p>
      </div>
    <% } %>

  </div>

  <footer class="bg-[#111] border-t border-[#222] py-8 text-center text-xs text-gray-600">
    <p>&copy; 2025 KomikCast Clone Project.</p>
  </footer>

</body>
</html>