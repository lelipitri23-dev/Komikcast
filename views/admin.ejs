<!DOCTYPE html>
<html lang="id">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Dashboard - KomikCast </title>
  <script src="https://cdn.tailwindcss.com/3.4.17"></script>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  
  <style>
    body { font-family: 'Poppins', sans-serif; background-color: #141414; color: #cbd5e1; }
    /* Scrollbar Table */
    .table-container::-webkit-scrollbar { height: 8px; }
    .table-container::-webkit-scrollbar-track { background: #1e1e1e; }
    .table-container::-webkit-scrollbar-thumb { background: #334155; border-radius: 4px; }
  </style>
</head>

<body class="min-h-screen flex flex-col">

  <nav class="bg-[#1e1e1e] border-b border-[#333] sticky top-0 z-50">
    <div class="container mx-auto px-4 h-16 flex items-center justify-between">
      <div class="flex items-center gap-2">
        <i class="fas fa-user-shield text-blue-500 text-xl"></i>
        <h1 class="text-lg md:text-xl font-bold text-white tracking-wide">ADMIN PANEL</h1>
      </div>
      
      <div class="flex gap-3">
        <a href="/" class="text-xs md:text-sm bg-[#333] hover:bg-gray-600 text-white px-3 py-2 rounded transition flex items-center gap-2">
          <i class="fas fa-home"></i> <span class="hidden md:inline">Website</span>
        </a>
        <a href="/logout" class="text-xs md:text-sm bg-red-600 hover:bg-red-700 text-white px-3 py-2 rounded transition flex items-center gap-2">
          <i class="fas fa-sign-out-alt"></i> <span class="hidden md:inline">Logout</span>
        </a>
      </div>
    </div>
  </nav>

  <div class="container mx-auto px-4 py-8 max-w-5xl flex-grow">

    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
      <div class="bg-[#1e1e1e] p-6 rounded-lg border border-[#333] shadow-lg flex items-center justify-between">
        <div>
          <p class="text-gray-500 text-xs uppercase font-bold mb-1">Total Komik Aktif</p>
          <h2 class="text-3xl font-black text-white"><%= mangas.length %></h2>
        </div>
        <div class="bg-blue-600/20 p-4 rounded-full text-blue-500">
          <i class="fas fa-book text-2xl"></i>
        </div>
      </div>
      
      <div class="bg-[#1e1e1e] p-6 rounded-lg border border-[#333] shadow-lg flex items-center justify-between">
        <div>
          <p class="text-gray-500 text-xs uppercase font-bold mb-1">Status Server</p>
          <h2 class="text-lg font-bold text-green-400 flex items-center gap-2">
            <span class="w-3 h-3 bg-green-500 rounded-full animate-pulse"></span> Online
          </h2>
        </div>
        <div class="bg-green-600/20 p-4 rounded-full text-green-500">
          <i class="fas fa-server text-2xl"></i>
        </div>
      </div>
    </div>

    <div class="bg-[#1e1e1e] rounded-lg border border-[#333] shadow-lg overflow-hidden mb-8">
      <div class="bg-[#252525] px-6 py-4 border-b border-[#333]">
        <h2 class="text-lg font-bold text-white flex items-center gap-2">
          <i class="fas fa-plus-circle text-blue-500"></i> Scrape Komik Baru
        </h2>
      </div>
      
      <div class="p-6">
        <form action="/admin/add-manga" method="POST" class="flex flex-col gap-4">
          <div class="relative w-full">
            <i class="fas fa-link absolute left-4 top-4 text-gray-500"></i>
            <textarea name="urls" rows="5" placeholder="Paste URL Komik di sini...&#10;Satu URL per baris&#10;Contoh:&#10;https://web.com/komik-a&#10;https://web.com/komik-b" 
                   class="w-full bg-[#141414] border border-[#333] text-white pl-10 pr-4 py-3 rounded focus:outline-none focus:border-blue-500 transition resize-y text-sm font-mono leading-relaxed" required></textarea>
          </div>
          <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded transition flex items-center justify-center gap-2 shadow-lg shadow-blue-900/20 w-full md:w-auto self-end">
            <i class="fas fa-robot"></i> Proses Semua URL
          </button>
        </form>
        <p class="text-[11px] text-gray-500 mt-3">
          <i class="fas fa-info-circle mr-1"></i> Masukkan satu URL per baris. Sistem akan memprosesnya secara antrian.
        </p>
      </div>
    </div>

    <div class="bg-[#1e1e1e] rounded-lg border border-[#333] shadow-lg overflow-hidden">
      <div class="bg-[#252525] px-6 py-4 border-b border-[#333] flex justify-between items-center">
        <h2 class="text-lg font-bold text-white flex items-center gap-2">
          <i class="fas fa-list text-yellow-500"></i> Daftar Komik (Auto-Update)
        </h2>
        <span class="text-xs bg-yellow-500/10 text-yellow-500 px-2 py-1 rounded border border-yellow-500/20">
          Cron Job Active
        </span>
      </div>

      <div class="overflow-x-auto table-container">
        <table class="w-full text-left border-collapse">
          <thead class="bg-[#1a1a1a] text-gray-400 text-xs uppercase tracking-wider">
            <tr>
              <th class="p-4 border-b border-[#333] font-semibold">Judul Komik</th>
              <th class="p-4 border-b border-[#333] font-semibold text-center">Tipe</th>
              <th class="p-4 border-b border-[#333] font-semibold">Update Terakhir</th>
              <th class="p-4 border-b border-[#333] font-semibold text-right">Action</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-[#333] text-sm">
            <% mangas.forEach(m => { %>
              <tr class="hover:bg-[#252525] transition group">
                <td class="p-4">
                  <div class="font-bold text-white group-hover:text-blue-400 transition mb-1 line-clamp-1">
                    <%= m.title %>
                  </div>
                  <div class="text-[10px] text-gray-600 font-mono"><%= m.slug %></div>
                </td>
                
                <td class="p-4 text-center">
                  <span class="px-2 py-1 rounded text-[10px] font-bold border 
                    <%= m.type.toLowerCase().includes('manhwa') ? 'bg-purple-900/30 text-purple-400 border-purple-800' : 
                       (m.type.toLowerCase().includes('manhua') ? 'bg-red-900/30 text-red-400 border-red-800' : 'bg-gray-700 text-gray-300 border-gray-600') %>">
                    <%= m.type %>
                  </span>
                </td>

                <td class="p-4 text-gray-400 text-xs whitespace-nowrap">
                  <i class="far fa-clock mr-1 text-gray-600"></i>
                  <%= new Date(m.lastUpdated).toLocaleDateString('id-ID', { day: 'numeric', month: 'short', year: 'numeric', hour: '2-digit', minute: '2-digit' }) %>
                </td>

                <td class="p-4 text-right">
                  <a href="/komik/<%= m.slug %>" target="_blank" class="text-gray-500 hover:text-white transition" title="Lihat">
                    <i class="fas fa-external-link-alt"></i>
                  </a>
                </td>
              </tr>
            <% }) %>
            
            <% if(mangas.length === 0) { %>
              <tr>
                <td colspan="4" class="p-8 text-center text-gray-500 italic">
                  Belum ada data komik. Silakan scrape URL baru.
                </td>
              </tr>
            <% } %>

          </tbody>
        </table>
      </div>
    </div>
    
  </div>

</body>
</html>